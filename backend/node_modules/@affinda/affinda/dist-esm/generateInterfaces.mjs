#!/usr/bin/env node
import { __awaiter } from "tslib";
import fs from 'fs';
import { compile } from 'json-schema-to-typescript';
import fetch from 'node-fetch';
import path from 'path';
import process from 'process';
import readlineSync from 'readline-sync';
import yargs from 'yargs';
import { hideBin } from 'yargs/helpers';
function fetchSchema(apiKey, baseUrl, docTypeId, title) {
    return __awaiter(this, void 0, void 0, function* () {
        const url = `${baseUrl}/v3/document_types/${docTypeId}/json_schema${title ? `?title=${encodeURIComponent(title)}` : ''}`;
        const headers = { Authorization: `Bearer ${apiKey}` };
        const response = yield fetch(url, { headers });
        if (!response.ok) {
            console.error(`Error contacting API: ${response.status} ${response.statusText}`);
            process.exit(1);
        }
        try {
            return (yield response.json());
        }
        catch (err) {
            console.error(`Unexpected response JSON: ${err}`);
            process.exit(1);
        }
    });
}
function getDocTypeIds(apiKey, baseUrl, organizationId, workspaceId) {
    return __awaiter(this, void 0, void 0, function* () {
        const headers = { Authorization: `Bearer ${apiKey}` };
        let params = '';
        if (organizationId !== undefined) {
            params = `organization=${organizationId}`;
        }
        else if (workspaceId !== undefined) {
            params = `workspace=${workspaceId}`;
        }
        else {
            throw new Error('Either organizationId or workspaceId must be provided.');
        }
        const url = `${baseUrl}/v3/document_types?${params}`;
        const response = yield fetch(url, { headers });
        if (!response.ok) {
            console.error(`Error fetching document types: ${response.status} ${response.statusText}`);
            process.exit(1);
        }
        const payload = (yield response.json());
        let documentTypeIds = payload.map((docType) => docType.identifier);
        // Fallback for portal v2 users whose collections are not yet migrated to be compatible with v3
        if (workspaceId !== undefined && documentTypeIds.length === 0) {
            const collectionUrl = `${baseUrl}/v3/collections?workspace=${workspaceId}`;
            const collectionResponse = yield fetch(collectionUrl, { headers });
            if (!collectionResponse.ok) {
                console.error(`Error fetching collections: ${collectionResponse.status} ${collectionResponse.statusText}`);
                process.exit(1);
            }
            const collectionPayload = (yield collectionResponse.json());
            documentTypeIds = collectionPayload.map((collection) => collection.identifier);
        }
        return documentTypeIds;
    });
}
function writeInterfaces(tsCode, fileStem, modelsDir) {
    return __awaiter(this, void 0, void 0, function* () {
        yield fs.promises.mkdir(modelsDir, { recursive: true });
        const modelPath = path.join(modelsDir, `${fileStem}.ts`);
        const finalCode = tsCode.trimEnd() + '\n';
        const existed = fs.existsSync(modelPath);
        yield fs.promises.writeFile(modelPath, finalCode, { encoding: 'utf8' });
        console.log(existed ? `Overwrote existing model at ${modelPath}.` : `Created model file ${modelPath}.`);
        return modelPath;
    });
}
// ---------------------------------------------------------------------------
// CLI entry
// ---------------------------------------------------------------------------
(() => __awaiter(void 0, void 0, void 0, function* () {
    const argv = yield yargs(hideBin(process.argv))
        .option('base-url', {
        type: 'string',
        default: 'https://api.affinda.com',
        describe: 'Affinda API base URL.',
    })
        .option('organization-id', {
        type: 'string',
        describe: 'Generate models for all document types in the organization.',
    })
        .option('workspace-id', {
        type: 'string',
        describe: 'Generate models for all document types (collections) in the workspace.',
    })
        .option('document-type-id', {
        type: 'string',
        describe: 'Generate models for a specific document type.',
    })
        .option('collection-id', {
        type: 'string',
        describe: 'Alias for --document-type-id.',
    })
        .option('interface-name', {
        type: 'string',
        describe: 'Specify interface name to generate. Only valid when --document-type-id is provided.',
    })
        .option('output-dir', {
        type: 'string',
        default: path.resolve('./affinda-interfaces'),
        describe: 'Directory in which to output the generated models.',
    })
        .strict()
        .help()
        .parse();
    const baseUrl = argv['base-url'].replace(/\/$/, '');
    const organizationId = argv['organization-id'];
    const workspaceId = argv['workspace-id'];
    const docTypeIdInput = argv['document-type-id'];
    const collectionIdInput = argv['collection-id'];
    const interfaceName = argv['interface-name'];
    const outputDir = path.resolve(argv['output-dir']);
    const docTypeId = docTypeIdInput || collectionIdInput;
    // Validate inputs
    if ([organizationId, workspaceId, docTypeId].filter(Boolean).length !== 1) {
        console.error('Please provide exactly one of --organization-id, --workspace-id, or --document-type-id.');
        process.exit(1);
    }
    if (interfaceName && !docTypeId) {
        console.error('--interface-name can only be used when --document-type-id is provided.');
        process.exit(1);
    }
    // Prompt for API key
    let apiKey = process.env.AFFINDA_API_KEY;
    if (!apiKey) {
        apiKey = readlineSync.question('Enter your API key (required when AFFINDA_API_KEY is not set): ', { hideEchoBack: true });
        if (!apiKey || !apiKey.trim()) {
            console.error('API key is required.');
            process.exit(1);
        }
        apiKey = apiKey.trim();
    }
    // Determine document type IDs to process
    let docTypeIds;
    if (docTypeId) {
        docTypeIds = [docTypeId];
    }
    else {
        docTypeIds = yield getDocTypeIds(apiKey, baseUrl, organizationId, workspaceId);
        if (!docTypeIds.length) {
            console.error('No document types found for the provided organization ID.');
            process.exit(1);
        }
    }
    let anyWritten = false;
    for (const id of docTypeIds) {
        const schema = yield fetchSchema(apiKey, baseUrl, id, interfaceName);
        const title = interfaceName || schema.title;
        const tsCode = yield compile(schema, title);
        const writtenPath = yield writeInterfaces(tsCode, title, outputDir);
        anyWritten = anyWritten || !!writtenPath;
    }
    if (anyWritten) {
        console.log('\nAll requested TypeScript interfaces processed.');
    }
}))();
//# sourceMappingURL=generateInterfaces.js.map